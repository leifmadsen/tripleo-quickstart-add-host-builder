---
- include_tasks: quickstart.yml
- include_tasks: quickstart-extras-undercloud.yml
- include_tasks: quickstart-extras-overcloud-prep.yml
- include_tasks: quickstart-extras-overcloud.yml
- include_tasks: quickstart-extras-validate.yml


# Add the overcloud nodes to the generated inventory.
- name:  Inventory the overcloud
  hosts: undercloud
  gather_facts: yes
  vars:
      inventory: all
  roles:
    - tripleo-inventory
    - add-host-builder
